@model RegisterViewModel

@{
    ViewData["Title"] = "Register";
    Layout = "_LayoutAuth";
}

<style>
    :root {
        --primary: #143f63;
        --primary-dark: #0f3350;
        --primary-light: #1d4f7c;
        --bg: #f4f8fc;
        --surface: #ffffff;
        --surface-soft: #eef4f9;
        --text: #1e293b;
        --muted: #64748b;
        --border: #d7e2ed;
    }

    body {
        background: var(--bg);
    }

    .auth-page {
        min-height: 100vh;
        display: flex;
        align-items: center;
        justify-content: center;
        padding: 24px;
    }

    .auth-card {
        width: 100%;
        max-width: 1100px;
        border: none;
        border-radius: 28px;
        overflow: hidden;
        box-shadow: 0 20px 50px rgba(20, 63, 99, 0.14);
    }

    .auth-left {
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
        color: #fff;
        padding: 60px 45px;
        position: relative;
    }

        .auth-left::before {
            content: "";
            position: absolute;
            width: 280px;
            height: 280px;
            border-radius: 50%;
            background: rgba(255, 255, 255, 0.08);
            top: -80px;
            right: -80px;
        }

    .brand-logo {
        width: 80px;
        height: 80px;
        object-fit: cover;
        border-radius: 50%;
        border: 4px solid rgba(255, 255, 255, 0.2);
        background: #fff;
    }

    .auth-right {
        background: #fff;
        padding: 50px 45px;
    }

    .form-control {
        height: 52px;
        border-radius: 14px;
        border: 1px solid var(--border);
        padding-left: 16px;
    }

        .form-control:focus {
            box-shadow: none;
            border-color: var(--primary);
        }

    .input-group-text {
        border-radius: 0 14px 14px 0;
    }

    .submit-btn {
        height: 52px;
        border-radius: 14px;
        background: linear-gradient(135deg, var(--primary) 0%, var(--primary-light) 100%);
        border: none;
        font-weight: 600;
        transition: .25s;
    }

        .submit-btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 12px 24px rgba(20, 63, 99, .22);
        }

    .small-link {
        color: var(--primary);
        text-decoration: none;
        font-weight: 600;
    }

        .small-link:hover {
            text-decoration: underline;
        }

    .role-card {
        border: 2px solid #e9eef5;
        border-radius: 16px;
        padding: 18px;
        cursor: pointer;
        transition: .25s;
        background: #fff;
    }

        .role-card:hover {
            border-color: var(--primary);
            transform: translateY(-2px);
        }

    .btn-check:checked + .role-card {
        border-color: var(--primary);
        background: #eef5ff;
    }
</style>

<div class="auth-page">
    <div class="card auth-card">
        <div class="row g-0">

            <!-- LEFT -->
            <div class="col-lg-5 d-none d-lg-flex flex-column justify-content-between auth-left">
                <div>
                    <div class="d-flex align-items-center gap-3 mb-5">
                        <img class="brand-logo"
                             src="~/image/Logo.png"
                             alt="TalentBridge" />

                        <div>
                            <h3 class="fw-bold mb-1">TalentBridge</h3>
                            <div class="small opacity-75">Freelancer Job Matching System</div>
                        </div>
                    </div>

                    <h1 class="fw-bold mb-4" style="font-size: 42px; line-height: 1.1;">
                        Join<br />
                        TalentBridge
                    </h1>

                    <p class="opacity-90" style="line-height: 1.7; max-width: 360px;">
                        Tạo tài khoản để bắt đầu kết nối với Freelancer hoặc Employer,
                        quản lý dự án và mở rộng cơ hội làm việc.
                    </p>
                </div>

                <div class="mt-5">
                    <div class="d-flex gap-4 flex-wrap">
                        <div>
                            <h3 class="fw-bold mb-1">Fast</h3>
                            <small>Quick signup</small>
                        </div>
                        <div>
                            <h3 class="fw-bold mb-1">Safe</h3>
                            <small>Secure access</small>
                        </div>
                        <div>
                            <h3 class="fw-bold mb-1">Easy</h3>
                            <small>Simple flow</small>
                        </div>
                    </div>
                </div>
            </div>

            <!-- RIGHT -->
            <div class="col-lg-7 auth-right">

                <div class="mb-4">
                    <h2 class="fw-bold mb-2">Create Account ✨</h2>
                    <p class="text-muted mb-0">Register to continue</p>
                </div>

                <form asp-action="Register" method="post">
                    @Html.AntiForgeryToken()

                    <!-- ROLE -->
                    <div class="mb-4">
                        <label class="fw-semibold mb-3 d-block">Select Role</label>

                        <div class="row g-3">
                            <div class="col-md-6">
                                <input type="radio"
                                       class="btn-check"
                                       name="Role"
                                       id="roleFreelancer"
                                       value="3"
                                       checked>


                                <label class="role-card w-100" for="roleFreelancer">
                                    <div class="d-flex align-items-center gap-3">
                                        <img src="~/image/anhfrelancer.png"
                                             alt="Freelancer"
                                             width="50"
                                             height="50"
                                             >
                                        <div>
                                            <h6 class="mb-1 fw-bold">Freelancer</h6>
                                            <small class="text-muted">Find jobs & projects</small>
                                        </div>
                                    </div>
                                </label>


                            </div>

                            <div class="col-md-6">
                                <input type="radio"
                                       class="btn-check"
                                       name="Role"
                                       id="roleEmployer"
                                       value="2">

                                <label class="role-card w-100" for="roleEmployer">
                                    <div class="d-flex align-items-center gap-3">

                                        <img src="~/image/employer.png"
                                             alt="Employer"
                                             width="50"
                                             height="50">

                                        <div>
                                            <h6 class="mb-1 fw-bold">Employer</h6>
                                            <small class="text-muted">Hire top freelancers</small>
                                        </div>

                                    </div>
                                </label>
                            </div>
                        </div>
                    </div>

                    <div class="row g-3">
                        <div class="col-md-6">
                            <label asp-for="Name" class="fw-semibold mb-2">Name</label>
                            <div class="input-group mb-1">
                                <input asp-for="Name" class="form-control" placeholder="Name" maxlength="255">
                                <span class="input-group-text bg-white">
                                    <i class="fas fa-user text-muted"></i>
                                </span>
                            </div>
                            <span asp-validation-for="Name" class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Email" class="fw-semibold mb-2">Email</label>
                            <div class="input-group mb-1">
                                <input asp-for="Email" type="email" class="form-control" placeholder="Email">
                                <span class="input-group-text bg-white">
                                    <i class="fas fa-envelope text-muted"></i>
                                </span>
                            </div>
                            <span asp-validation-for="Email" class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="Password" class="fw-semibold mb-2">Password</label>
                            <div class="input-group mb-1">
                                <input asp-for="Password" type="password" class="form-control" placeholder="Password">
                                <span class="input-group-text bg-white">
                                    <i class="fas fa-lock text-muted"></i>
                                </span>
                            </div>
                            <span asp-validation-for="Password" class="text-danger small"></span>
                        </div>

                        <div class="col-md-6">
                            <label asp-for="ConfirmPassword" class="fw-semibold mb-2">Confirm Password</label>
                            <div class="input-group mb-1">
                                <input asp-for="ConfirmPassword" type="password" class="form-control" placeholder="Confirm Password">
                                <span class="input-group-text bg-white">
                                    <i class="fas fa-lock text-muted"></i>
                                </span>
                            </div>
                            <span asp-validation-for="ConfirmPassword" class="text-danger small"></span>
                        </div>
                    </div>

                    <div asp-validation-summary="All" class="text-danger mt-3"></div>

                    @if (ViewBag.Error != null)
                    {
                        <div class="alert alert-danger mt-3 mb-0">
                            @ViewBag.Error
                        </div>
                    }

                    <button type="submit" class="btn btn-primary submit-btn w-100 mt-4">
                        Register
                    </button>

                    <div class="mt-4 text-center">
                        <span class="text-muted">Already have an account? </span>
                        <a asp-action="Login" class="small-link">Login here</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</div>